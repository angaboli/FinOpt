# API Configuration
# Set in app.json > extra > apiUrl or override via environment
API_URL=http://localhost:8000/api/v1

# App Configuration
APP_ENV=development    # development | staging | production

# For production builds, update app.json extra.apiUrl to your deployed API URL
# Example: https://api.finopt.com/api/v1
